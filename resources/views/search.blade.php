<!doctype html>
<html lang="{{ app()->getLocale() }}">
  <head>
    <meta charset="utf-8">
    <title>Simbirsk toys</title>
  </head>

  <body>
    <div>
      <h1>Work</h1>
     <!-- <ul>
          @foreach($users->users as $user)
                  <li>{{ $user->name }}</li>
          @endforeach
        </ul>-->
    </div>
  </body>
</html>